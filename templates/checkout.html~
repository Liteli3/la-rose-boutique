{% extends 'base.html' %}
{% load static %}

{% block title %}Paiement - LA ROSE BOUTIQUE{% endblock %}

{% block extra_head %}
<style>
    /* Styles spécifiques pour la page de paiement */
    .checkout-page {
        background-color: #ebf5ff;
        min-height: 100vh;
        padding: 20px;
        display: flex;
        justify-content: center;
        align-items: flex-start;
    }

    .checkout-container {
        background-color: white;
        padding: 30px;
        border-radius: 12px;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        width: 100%;
        max-width: 600px;
        margin: 20px 0;
    }

    h1 {
        color: #333;
        text-align: center;
        margin-bottom: 30px;
        font-size: 1.8rem;
        border-bottom: 2px solid #f0f0f0;
        padding-bottom: 15px;
    }

    h2 {
        color: #444;
        margin: 25px 0 15px 0;
        font-size: 1.3rem;
        padding-bottom: 8px;
        border-bottom: 1px solid #eee;
    }

    .form-group {
        margin-bottom: 20px;
    }

    label {
        display: block;
        margin-bottom: 8px;
        font-weight: 600;
        color: #555;
        font-size: 0.95rem;
    }

    input[type="text"],
    input[type="tel"],
    textarea,
    select {
        width: 100%;
        padding: 12px;
        border: 2px solid #e1e5e9;
        border-radius: 8px;
        box-sizing: border-box;
        font-size: 1rem;
        transition: all 0.3s ease;
        background-color: #fafbfc;
    }

    input[type="text"]:focus,
    input[type="tel"]:focus,
    textarea:focus,
    select:focus {
        outline: none;
        border-color: #4CAF50;
        background-color: white;
        box-shadow: 0 0 0 3px rgba(76, 175, 80, 0.1);
    }

    textarea {
        resize: vertical;
        min-height: 80px;
        font-family: inherit;
    }

    select {
        cursor: pointer;
        appearance: none;
        background-image: url("data:image/svg+xml;charset=US-ASCII,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 4 5'><path fill='%23666' d='M2 0L0 2h4zm0 5L0 3h4z'/></svg>");
        background-repeat: no-repeat;
        background-position: right 12px center;
        background-size: 12px;
        padding-right: 40px;
    }

    .submit-btn {
        width: 100%;
        padding: 15px;
        background: linear-gradient(135deg, #4CAF50, #45a049);
        color: white;
        border: none;
        border-radius: 8px;
        cursor: pointer;
        font-size: 1.1rem;
        font-weight: 600;
        transition: all 0.3s ease;
        box-shadow: 0 4px 12px rgba(76, 175, 80, 0.3);
        margin-top: 10px;
    }

    .submit-btn:hover {
        background: linear-gradient(135deg, #45a049, #3d8b40);
        transform: translateY(-2px);
        box-shadow: 0 6px 16px rgba(76, 175, 80, 0.4);
    }

    .submit-btn:active {
        transform: translateY(0);
    }

    .back-link {
        display: block;
        text-align: center;
        margin-top: 25px;
        color: #666;
        text-decoration: none;
        font-weight: 500;
        padding: 12px;
        border: 2px solid #e1e5e9;
        border-radius: 8px;
        transition: all 0.3s ease;
        background-color: #f8f9fa;
    }

    .back-link:hover {
        color: #333;
        background-color: #e9ecef;
        border-color: #ccc;
        text-decoration: none;
    }

    /* Style pour les messages d'erreur */
    .error-message {
        color: #dc3545;
        font-size: 0.85rem;
        margin-top: 5px;
        display: block;
    }

    /* Indicateur de champ requis */
    .required::after {
        content: " *";
        color: #dc3545;
    }

    /* Style pour le résumé de commande (optionnel) */
    .order-summary {
        background: #f8f9fa;
        padding: 20px;
        border-radius: 8px;
        margin-bottom: 25px;
        border-left: 4px solid #4CAF50;
    }

    .order-summary h3 {
        margin-top: 0;
        color: #333;
        font-size: 1.1rem;
    }

    /* ********************************************************** */
    /* RESPONSIVE DESIGN */
    /* ********************************************************** */

    /* Tablettes */
    @media (max-width: 768px) {
        .checkout-page {
            padding: 15px;
        }

        .checkout-container {
            padding: 25px;
            margin: 15px 0;
        }

        h1 {
            font-size: 1.6rem;
            margin-bottom: 25px;
        }

        h2 {
            font-size: 1.2rem;
            margin: 20px 0 12px 0;
        }
    }

    /* Mobiles */
    @media (max-width: 480px) {
        .checkout-page {
            padding: 10px;
            align-items: stretch;
        }

        .checkout-container {
            padding: 20px;
            margin: 10px 0;
            border-radius: 8px;
        }

        h1 {
            font-size: 1.4rem;
            margin-bottom: 20px;
            padding-bottom: 12px;
        }

        h2 {
            font-size: 1.1rem;
            margin: 18px 0 10px 0;
        }

        .form-group {
            margin-bottom: 16px;
        }

        input[type="text"],
        input[type="tel"],
        textarea,
        select {
            padding: 10px;
            font-size: 16px; /* Évite le zoom automatique sur iOS */
            border-width: 1px;
        }

        .submit-btn {
            padding: 14px;
            font-size: 1rem;
        }

        .back-link {
            margin-top: 20px;
            padding: 10px;
        }
    }

    /* Très petits mobiles */
    @media (max-width: 360px) {
        .checkout-container {
            padding: 15px;
        }

        h1 {
            font-size: 1.3rem;
        }

        h2 {
            font-size: 1rem;
        }

        label {
            font-size: 0.9rem;
        }

        input[type="text"],
        input[type="tel"],
        textarea,
        select {
            padding: 8px;
        }
    }

    /* Mode paysage mobile */
    @media (max-height: 500px) and (orientation: landscape) {
        .checkout-page {
            padding: 10px;
            align-items: flex-start;
        }

        .checkout-container {
            margin: 10px 0;
            padding: 20px;
        }

        .form-group {
            margin-bottom: 12px;
        }
    }

    /* Amélioration de l'accessibilité */
    @media (prefers-reduced-motion: reduce) {
        * {
            transition: none !important;
            animation: none !important;
        }
    }

    /* Mode sombre (optionnel) */
    @media (prefers-color-scheme: dark) {
        .checkout-page {
            background-color: #1a1a1a;
        }

        .checkout-container {
            background-color: #2d2d2d;
            color: #e0e0e0;
        }

        h1, h2 {
            color: #f0f0f0;
        }

        label {
            color: #cccccc;
        }

        input[type="text"],
        input[type="tel"],
        textarea,
        select {
            background-color: #3d3d3d;
            border-color: #555;
            color: #e0e0e0;
        }

        .back-link {
            background-color: #3d3d3d;
            border-color: #555;
            color: #cccccc;
        }
    }
</style>
{% endblock extra_head %}

{% block content %}
<div class="checkout-page">
    <div class="checkout-container">
        <h1>Finaliser la commande</h1>

        <!-- Optionnel: Ajouter un résumé de commande -->
        <div class="order-summary">
            <h3>📦 Votre commande</h3>
            <p>Articles: {{ cart_items_count }} produit(s)</p>
            <p><strong>Total: {{ cart_total_price }} LR</strong></p>
        </div>

        <form method="POST" action="{% url 'checkout' %}">
            {% csrf_token %}

            <h2>1. Vos coordonnées</h2>

            <div class="form-group">
                <label for="id_full_name" class="required">Nom complet :</label>
                <input type="text" id="id_full_name" name="full_name" required
                       placeholder="Ex: Jean Dupont" aria-describedby="name-help">
                <small id="name-help" class="error-message" style="display: none;">
                    Veuillez entrer votre nom complet
                </small>
            </div>

            <div class="form-group">
                <label for="id_phone_number" class="required">Téléphone :</label>
                <input type="tel" id="id_phone_number" name="phone_number" required
                       placeholder="Ex: +243 99 123 4567" pattern="[\+]?[0-9\s\-]+">
                <small class="error-message" style="display: none;">
                    Format: +243 XX XXX XXXX ou 0XX XXX XXXX
                </small>
            </div>

            <div class="form-group">
                <label for="id_address_line_1" class="required">Adresse de livraison :</label>
                <textarea id="id_address_line_1" name="address_line_1" rows="3" required
                          placeholder="Rue, avenue, quartier, commune, ville..."></textarea>
            </div>

            <h2>2. Mode de paiement</h2>
            <div class="form-group">
                <label for="id_payment_method" class="required">Choisissez votre méthode de paiement :</label>
                <select id="id_payment_method" name="payment_method" required>
                    <option value="" disabled selected>-- Sélectionner --</option>
                    <option value="Cash">💵 Cash (Argent liquide)</option>
                    <option value="M-pesa">📱 M-pesa</option>
                    <option value="Airtel money">📶 Airtel Money</option>
                    <option value="Orange money">🍊 Orange Money</option>
                    <option value="Afri-money">🌍 Afri-money</option>
                </select>
            </div>

            <button type="submit" class="submit-btn">✅ Confirmer la commande</button>
            <a href="{% url 'cart' %}" class="back-link">← Revenir au panier</a>
        </form>
    </div>
</div>
{% endblock content %}

{% block extra_js %}
<script>
    document.addEventListener('DOMContentLoaded', function() {
        // Validation basique des champs
        const form = document.querySelector('form');
        const phoneInput = document.getElementById('id_phone_number');
        const nameInput = document.getElementById('id_full_name');

        // Validation du téléphone
        phoneInput.addEventListener('input', function(e) {
            const value = e.target.value.replace(/[^\d+]/g, '');
            e.target.value = value;
        });

        // Validation à la soumission
        form.addEventListener('submit', function(e) {
            let isValid = true;

            // Validation nom
            if (!nameInput.value.trim()) {
                isValid = false;
                nameInput.style.borderColor = '#dc3545';
            } else {
                nameInput.style.borderColor = '#4CAF50';
            }

            // Validation téléphone
            const phoneValue = phoneInput.value.replace(/\s/g, '');
            if (phoneValue.length < 9) {
                isValid = false;
                phoneInput.style.borderColor = '#dc3545';
            } else {
                phoneInput.style.borderColor = '#4CAF50';
            }

            if (!isValid) {
                e.preventDefault();
                alert('Veuillez corriger les erreurs dans le formulaire.');
            }
        });

        // Reset des bordures lors de la saisie
        const inputs = form.querySelectorAll('input, select, textarea');
        inputs.forEach(input => {
            input.addEventListener('input', function() {
                this.style.borderColor = '#e1e5e9';
            });
        });
    });
</script>
{% endblock extra_js %}
